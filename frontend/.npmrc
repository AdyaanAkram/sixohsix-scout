# react-scripts 5 pulls @typescript-eslint 5.x (peer: eslint <=8) while this app
# uses eslint 9, so a strict install (npm 7+, e.g. Cloudflare Pages CI) fails
# ERESOLVE. The conflict is lint-only and does not affect the build output.
legacy-peer-deps=true
